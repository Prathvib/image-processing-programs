import cv2  
img = cv2.imread('cat.jpg')
img_h = cv2.flip(img,0)
cv2.imshow("Vertical Flip",img_h)
cv2.waitKey(0)

img_h = cv2.flip(img, 1)
cv2.imshow("Horizontal Flip", img_h)
cv2.waitKey(0)
cv2.destroyAllWindows()
